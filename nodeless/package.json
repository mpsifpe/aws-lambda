{
  "name": "nodeless",
  "version": "1.0.0",
  "description": "",
  "main": "optimize.js",
  "dependencies": {
    "sharp": "^0.29.3"
  },
  "devDependencies": {},
  "scripts": {
    "predeploy": "rm -rf node_modules/sharp && npm install --arch=x64 --platform=linux --target=8.10.0 sharp",
    "deploy": "serverless deploy --verbose",
    "postdeploy": "rm -rf node_modules/sharp && npm install sharp"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
